<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <title>Customer Profile</title>

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-image: url("../images/home/bg1.jpg");
            background-size: cover;
            font-family: Roboto;
            background-attachment: fixed;
            background-repeat: no-repeat;
            /* Optional: Background color for the page */
        }

        .glass-box {
            display: flex;
            position: relative;
            width: 900px;
            /* Adjust the width as needed */
            height: 600px;
            /* Adjust the height as needed */
            background-color: rgb(207, 210, 211);
            box-shadow: 5px 5px 5px 3px rgba(2, 4, 0, 0.1);
            /* Box shadow for the glass effect */
            backdrop-filter: blur(1000px);
            /* Blur for the frosted glass effect */
            overflow: hidden;
            /* Ensure the content does not overflow outside the glass-box */
            border-radius: 50px;
        }

        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 175px;
            height: 100%;
            padding: 20px;
            background-color: black;
            color: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            z-index: 1;
            /* Ensure the sidebar is above the glass-box content */
        }

        .sidebar-text {
            font-family: Roboto;
            color: white;
            font-weight: bold;
            cursor: pointer;
            margin-top: 20px;
            margin-left: 10px;
            margin-bottom: 10px;
            font-size: 17px;
            display: block;
        }

        .sidebar-text:hover {
            color: blue;
        }

        .HeadLine {
            margin-left: 410px;
            padding: 30px;
        }

        .formContent {
            flex-grow: 1;
            padding: 10px;
            margin-left: 330px;
        }

        .FormAttribute {
            padding: 2px;
            width: 100%;
        }

        .container .formContent .FormAttribute .FormInputBox {
            border-radius: 20px; /* Round the corners */
            background-color: #f0f0f0; /* Set background color */
            border: 1px solid #ccc; /* Add a border */
            padding: 10px; 
            width : 100%;
        }

        .btn-primary {
            padding: 10px;
            margin-top: 20px;
            margin-left: 110px;
        }

        .btn-success {
            padding: 10px;
            margin-top: 20px;
            margin-left: 90px;
        }

        .sidebar-logo {
            width: 100%;
            /* Make the image fill the entire width of the sidebar */
            max-height: 130px;
            /* Set a maximum height for the image */
            margin-bottom: 20px;
            /* Adjust as needed for spacing */
            border-radius: 50%;
        }

        .marginleftName {
            margin-left: 23px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftEmail {
            margin-left: 54px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftPhone {
            margin-left: 48px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftewallet {
            margin-left: 6px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftGender {
            margin-left: 43px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftStreet {
            margin-left: 5px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftPostCode {
            margin-left: 8px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }
        .marginleftCity {
            margin-left: 67px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

        .marginleftDivCon {
            margin-left: 38px;
            border-radius: 25px; /* change here */
            width: 15vw;
            text-align: center;
        }

    </style>
</head>

<body>

    <div class="glass-box">

        <div class="sidebar">
            <img src="../images/UserProfile/<%=PROFILE_PICTURE%>" alt="Your Image" class="sidebar-logo">
            <a class="sidebar-text" href="/home/<%= userID %>">Home</a>
            <a class="sidebar-text" href="/user/<%= userID %>">Your Profile</a>
            <a class="sidebar-text" href="/order/<%= userID %>">Order History</a>
            <a class="sidebar-text" href="/wishlist/<%= userID%>">Wishlist Products</a>
            <a class="sidebar-text" href="/OrderTrack/<%= userID%>">Track Your Last Order</a>
            <a class="sidebar-text" href="/Password/<%= userID%>">Change Password</a>

        </div>

        <div class="container">
            <h2 class="HeadLine">Your Profile</h2>

            <form class="formContent" action="/user/<%= userID %>" method="post"
                enctype="application/x-www-form-urlencoded">

                <div class="FormAttribute">
                    <label for="name"><strong>Full Name:</strong></label>
                    <input type="text" class="marginleftName" class="FormInputBox" id="name" name="name" value="<%=NAME%>" readonly>
                </div>
                
                <input style="display: none;" type="text" class="marginleftName" class="FormInputBox" id="profilePic" name="profilePic" value="<%=PROFILE_PICTURE%>" readonly>

                <div class="FormAttribute">
                    <label for="email"><strong>Email:</strong></label>
                    <input type="email" class="marginleftEmail" class="FormInputBox" id="email" name="email" value="<%= EMAIL %>" readonly>
                </div>

                <div class="FormAttribute">
                    <label for="phone"><strong>Phone:</strong></label>
                    <input type="tel" class="marginleftPhone" class="FormInputBox" id="phone" name="phone" value="<%= PHONE %>" readonly>
                </div>

                <div class="FormAttribute">
                    <label for="ewallet"><strong>E-Wallet Bal:</strong></label>
                    <input type="number" class="marginleftewallet" class="FormInputBox" id="ewallet" name="ewallet" value="<%= EWALLET %>" readonly>
                </div>

                <div class="FormAttribute">
                    <label for="phone"><strong>Gender:</strong></label>
                    <input type="tel" class="marginleftGender" class="FormInputBox" id="gender" name="gender" value="<%= GENDER %>" disabled>
                </div>

                <div class="FormAttribute">
                    <label for="description"><strong>Street Name:</strong></label>
                    <input type="text" class="marginleftStreet"  class="FormInputBox" id="street" name="street" value="<%= STREET %>" readonly>
                </div>

                <div class="FormAttribute">
                    <label for="description"><strong>Postal Code:</strong></label>
                    <input type="text" class="marginleftPostCode" class="FormInputBox" id="postCode" name="postCode" value="<%= POSTCODE %>"
                        readonly>
                </div>

                <div class="FormAttribute">
                    <label for="description"><strong>City:</strong></label>
                    <input type="text" class="marginleftCity" class="marginleftCity" class="FormInputBox" id="city" name="city" value="<%= CITY %>" readonly>
                </div>

                <div class="FormAttribute">
                    <label for="description"><strong>Division:</strong></label>
                    <input type="text" class="marginleftDivCon" class="FormInputBox" id="division" name="division" value="<%= DIVISION %>"
                        readonly>
                </div>

                <div class="FormAttribute">
                    <label for="description"><strong>Country:</strong></label>
                    <input type="text" class="marginleftDivCon" class="FormInputBox" id="country" name="country" value="<%= COUNTRY %>" readonly>
                </div>

                <button type="button" class="btn btn-primary" id="editBtn">Edit Information</button>
                <button type="submit" class="btn btn-success d-none" id="saveBtn">Save Changes</button>

            </form>

        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
</body>
<script>

    $(document).ready(function () {
        // Edit Information button click event
        $("#editBtn").on("click", function () {
            // Enable editing
            $("input[readonly]").prop("readonly", false);

            // Show Save Changes button and hide Edit Information button
            $("#saveBtn").removeClass("d-none");
            $(this).addClass("d-none");

        });

        $("#saveBtn").on("click", function () {
            // Submit the form when Save Changes button is clicked
            $("#profileForm").submit();
        });

        // Save Changes button click event
    });

</script>

<!-- <script>
    const userId = '<%= userID %>'; // Assuming you're passing userId from the backend
    document.getElementById('trackOrderLink').addEventListener('click', function (event) {
        event.preventDefault(); // Prevent default link behavior

        // Data to send in the request body
        const requestData = {
            userId: userId   // Use the userId from the backend
        };

        // Make AJAX POST request
        fetch('/OrderTrack', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(requestData)
        })
            .then(response => {
                // Handle response as needed
                console.log('Order tracking request sent.');
                // Optionally, redirect user or perform other actions
            })
            .catch(error => {
                // Handle error if needed
                console.error('Error sending order tracking request:', error);
            });
    });
</script> -->

</html>